{
  byte versionFromBuffer=buffer.get();
  if (versionFromBuffer == version) {
    return new CompressedLongsIndexedSupplier(buffer.getInt(),buffer.getInt(),GenericIndexed.readFromByteBuffer(buffer,CompressedLongBufferObjectStrategy.getBufferForOrder(order)));
  }
  throw new IAE("Unknown version[%s]",versionFromBuffer);
}
