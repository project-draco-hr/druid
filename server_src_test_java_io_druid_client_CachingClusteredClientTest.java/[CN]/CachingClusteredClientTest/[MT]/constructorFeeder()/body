{
  return Lists.transform(Lists.newArrayList(new RangeIterable(RANDOMNESS)),new Function<Integer,Object[]>(){
    @Override public Object[] apply(    @Nullable Integer input){
      return new Object[]{input};
    }
  }
);
}
