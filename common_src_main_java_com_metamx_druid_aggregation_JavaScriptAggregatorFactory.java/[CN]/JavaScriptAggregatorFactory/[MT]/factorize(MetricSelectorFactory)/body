{
  return new JavaScriptAggregator(name,Lists.transform(fieldNames,new com.google.common.base.Function<String,FloatMetricSelector>(){
    @Override public FloatMetricSelector apply(    @Nullable String s){
      return metricFactory.makeFloatMetricSelector(s);
    }
  }
),compiledScript);
}
