{
  if (retainMissing && !Strings.isNullOrEmpty(replaceMissing)) {
    final LookupExtractionFn lookupExtractionFn=new LookupExtractionFn(new MapLookupExtractor(ImmutableMap.of("foo","bar")),retainMissing,Strings.emptyToNull(replaceMissing),injective,false);
  }
 else {
    throw new IAE("Case not valid");
  }
}
