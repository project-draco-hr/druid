{
  HadoopConverterTask orig=new HadoopConverterTask(TASK_ID,DATA_SOURCE,INTERVAL,INDEX_SPEC,true,null,HADOOP_DEPENDENCY,DISTRIBUTED_CACHE,PRIORITY,OUTPUT_PATH,CLASSPATH_PREFIX,null);
  Assert.assertTrue(orig.isValidate());
}
