{
  LoaderPullerAdapter puller=new LoaderPullerAdapter(new File(config.getTaskDir(task),"fetched_segments"));
  return ImmutableMap.<String,SegmentPuller>builder().put("s3",puller).put("s3_union",puller).put("s3_zip",puller).build();
}
