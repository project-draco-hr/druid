{
  int minIndex=-1;
  float min=Float.MAX_VALUE;
  for (int k=0; k < lastValidIndex; ++k) {
    float value=deltas[k];
    if (value < min) {
      minIndex=k;
      min=value;
    }
  }
  return minIndex;
}
