{
  return Iterables.transform(Arrays.asList(CompressedObjectStrategy.CompressionStrategy.noNoneValues()),new Function<CompressedObjectStrategy.CompressionStrategy,Object[]>(){
    @Override public Object[] apply(    CompressedObjectStrategy.CompressionStrategy compressionStrategy){
      return new Object[]{compressionStrategy};
    }
  }
);
}
