{
  verifyState();
  return new RealtimePlumber(schema,config,metrics,emitter,conglomerate,segmentAnnouncer,queryExecutorService,dataSegmentPusher,segmentPublisher,serverView,indexMerger,indexIO,cache,cacheConfig,objectMapper);
}
