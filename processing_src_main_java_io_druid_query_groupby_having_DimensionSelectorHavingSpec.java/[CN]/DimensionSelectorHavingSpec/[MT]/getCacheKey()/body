{
  byte[] dimBytes=StringUtils.toUtf8(this.dimension);
  byte[] valBytes=StringUtils.toUtf8(this.value);
  return ByteBuffer.allocate(2 + dimBytes.length + valBytes.length).put(CACHE_KEY).put(dimBytes).put(STRING_SEPARATOR).put(valBytes).array();
}
