{
  try {
    this.client=HttpClientInit.createClient(new HttpClientConfig(1,SSLContext.getDefault(),Duration.ZERO),new Lifecycle());
  }
 catch (  Exception e) {
    throw Throwables.propagate(e);
  }
}
