{
  this.period=(period == null || period.equalsIgnoreCase("all")) ? new Period("P1000Y") : new Period(period);
  this.replicationFactor=(replicationFactor == null) ? 2 : replicationFactor;
  this.tier=tier;
}
