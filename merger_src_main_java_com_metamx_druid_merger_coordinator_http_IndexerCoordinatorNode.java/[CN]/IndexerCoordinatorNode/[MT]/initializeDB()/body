{
  if (dbConnectorConfig == null) {
    dbConnectorConfig=getConfigFactory().build(DbConnectorConfig.class);
  }
  if (dbi == null) {
    dbi=new DbConnector(Suppliers.ofInstance(dbConnectorConfig),null).getDBI();
  }
}
