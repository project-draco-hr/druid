{
  createTable(dbi,tableName,String.format("CREATE TABLE %s (id BIGINT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), " + "task_id VARCHAR(255) DEFAULT NULL, lock_payload CLOB, PRIMARY KEY (id))",tableName));
  createIndex(dbi,tableName,"task_lock_task_id","task_id");
}
