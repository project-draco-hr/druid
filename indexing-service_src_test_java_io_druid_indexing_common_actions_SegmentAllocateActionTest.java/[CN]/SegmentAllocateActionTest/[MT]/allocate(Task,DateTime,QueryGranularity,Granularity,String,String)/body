{
  final SegmentAllocateAction action=new SegmentAllocateAction(DATA_SOURCE,timestamp,queryGranularity,preferredSegmentGranularity,sequenceName,sequencePreviousId);
  return action.perform(task,taskActionTestKit.getTaskActionToolbox()).orNull();
}
