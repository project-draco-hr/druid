{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Token token=(Token)o;
  if (type != token.type) {
    return false;
  }
  return !(match != null ? !match.equals(token.match) : token.match != null);
}
