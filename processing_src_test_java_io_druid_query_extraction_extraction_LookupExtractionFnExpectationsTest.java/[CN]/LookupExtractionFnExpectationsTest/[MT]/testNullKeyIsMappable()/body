{
  final LookupExtractionFn lookupExtractionFn=new LookupExtractionFn(new MapLookupExtractor(ImmutableMap.of("","bar")),false,"REPLACE",false,false);
  Assert.assertEquals("bar",lookupExtractionFn.apply(null));
}
