{
  final LookupExtractionFn lookupExtractionFn=new LookupExtractionFn(new MapLookupExtractor(ImmutableMap.of("foo","bar")),true,null,false,false);
  Assert.assertNull(lookupExtractionFn.apply(null));
}
