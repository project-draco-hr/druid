{
  final LookupExtractionFn lookupExtractionFn=new LookupExtractionFn(new MapLookupExtractor(ImmutableMap.of("foo","")),false,"REPLACE",false,false);
  Assert.assertEquals("REPLACE",lookupExtractionFn.apply(null));
}
