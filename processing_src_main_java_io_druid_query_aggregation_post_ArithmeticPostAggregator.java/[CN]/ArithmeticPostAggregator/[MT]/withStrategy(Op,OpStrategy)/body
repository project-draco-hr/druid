{
  if (strategy.equals(OpStrategy.none)) {
    return baseOp;
  }
  return new Op(){
    @Override public double compute(    double lhs,    double rhs){
      return strategy.compute(baseOp,lhs,rhs);
    }
  }
;
}
