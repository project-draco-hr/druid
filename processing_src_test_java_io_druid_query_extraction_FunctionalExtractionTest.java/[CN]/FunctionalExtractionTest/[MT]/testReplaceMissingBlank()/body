{
  final String in="NOT PRESENT";
  final FunctionalExtraction exFn=new SimpleFunctionExtraction(fn,false,"",false);
  final String out=fn.apply(in);
  Assert.assertEquals(Strings.isNullOrEmpty(out) ? null : out,exFn.apply(in));
}
