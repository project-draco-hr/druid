{
  createTable(dbi,taskLogsTableName,String.format("CREATE TABLE `%s` (\n" + "  `id` bigint(20) NOT NULL AUTO_INCREMENT,\n" + "  `task_id` varchar(255) DEFAULT NULL,\n"+ "  `log_payload` longtext,\n"+ "  PRIMARY KEY (`id`),\n"+ "  KEY `task_id` (`task_id`)\n"+ ")",taskLogsTableName));
}
