{
  try {
    return spatialIndexes.get(dimension);
  }
 catch (  NullPointerException e) {
    log.warn(e,"NPE on dimension[%s] over interval[%s]",dimension,dataInterval);
    return new ImmutableRTree();
  }
}
