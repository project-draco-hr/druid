{
  try {
    return indexes.get(dimension)[valueIndex];
  }
 catch (  NullPointerException e) {
    log.warn(e,"NPE on dimension[%s], valueIndex[%d], with index over interval[%s]",dimension,valueIndex,dataInterval);
    return emptySet;
  }
}
