{
  giant.lock();
  try {
    Task task;
    log.info("Waiting for work...");
    while ((task=poll()) == null) {
      workMayBeAvailable.awaitNanos(1000000000L);
    }
    return task;
  }
  finally {
    giant.unlock();
  }
}
