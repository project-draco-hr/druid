{
  createTable(dbi,tableName,String.format("CREATE TABLE %1$s (\n" + "  id %2$s NOT NULL,\n" + "  task_id VARCHAR(255) DEFAULT NULL,\n"+ "  log_payload %3$s,\n"+ "  PRIMARY KEY (id)\n"+ ");\n"+ "CREATE INDEX idx_%1$s_task_id ON %1$s(task_id);",tableName,getSerialType(),getPayloadType()));
}
