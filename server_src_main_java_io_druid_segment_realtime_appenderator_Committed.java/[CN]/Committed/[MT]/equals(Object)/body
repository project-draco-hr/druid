{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Committed committed=(Committed)o;
  return Objects.equals(hydrants,committed.hydrants) && Objects.equals(metadata,committed.metadata);
}
