{
  try {
    pathChildrenCache.close();
    exec.shutdownNow();
    taskRunner.stop();
  }
 catch (  Exception e) {
    log.makeAlert(e,"Exception stopping WorkerTaskMonitor").addData("exception",e.toString()).emit();
  }
}
