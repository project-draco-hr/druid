{
  return makeQueryRunner(Lists.transform(segments,new Function<Segment,LogicalWrapper>(){
    @Override public LogicalWrapper apply(    Segment segment){
      return new LogicalWrapper(segment);
    }
  }
),factory);
}
