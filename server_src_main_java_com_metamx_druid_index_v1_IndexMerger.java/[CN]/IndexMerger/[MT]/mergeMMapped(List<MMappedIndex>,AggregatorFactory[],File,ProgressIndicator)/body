{
  return merge(Lists.transform(indexes,new Function<MMappedIndex,IndexableAdapter>(){
    @Override public IndexableAdapter apply(    @Nullable final MMappedIndex input){
      return new MMappedIndexAdapter(input);
    }
  }
),metricAggs,outDir,progress);
}
