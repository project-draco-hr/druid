{
  return toolChest.mergeSequencesUnordered(Sequences.simple(Iterables.transform(baseRunners,new Function<QueryRunner,Sequence<T>>(){
    @Override public Sequence<T> apply(    QueryRunner singleRunner){
      return singleRunner.run(query);
    }
  }
)));
}
