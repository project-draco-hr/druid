{
  return toolChest.mergeSequencesUnordered(Sequences.simple(Iterables.transform(baseRunners,new Function<QueryRunner<T>,Sequence<T>>(){
    @Override public Sequence<T> apply(    QueryRunner<T> singleRunner){
      return singleRunner.run(query);
    }
  }
)));
}
