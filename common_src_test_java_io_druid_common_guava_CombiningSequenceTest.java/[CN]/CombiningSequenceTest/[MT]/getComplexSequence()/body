{
  Ordering<Integer> alwaysSame=new Ordering<Integer>(){
    @Override public int compare(    Integer left,    Integer right){
      return 0;
    }
  }
;
  BinaryFn<Integer,Integer,Integer> plus=new BinaryFn<Integer,Integer,Integer>(){
    @Override public Integer apply(    Integer arg1,    Integer arg2){
      if (arg1 == null) {
        return arg2;
      }
      if (arg2 == null) {
        return arg1;
      }
      return arg1 + arg2;
    }
  }
;
  return CombiningSequence.create(Sequences.concat(ImmutableList.<Sequence<Integer>>of(CombiningSequence.create(Sequences.simple(ImmutableList.<Integer>of(3)),alwaysSame,plus),CombiningSequence.create(Sequences.simple(ImmutableList.of(5)),alwaysSame,plus))),alwaysSame,plus);
}
