{
  for (int limit=0; limit < expected.size() + 1; limit++) {
    final int expectedLimit=limit == 0 ? 1 : limit;
    testCombining(pairs,Lists.newArrayList(Iterables.limit(expected,expectedLimit)),limit);
  }
}
