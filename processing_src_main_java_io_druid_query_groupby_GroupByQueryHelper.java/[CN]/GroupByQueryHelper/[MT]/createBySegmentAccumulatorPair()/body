{
  List init=Lists.newArrayList();
  Accumulator<List,T> accumulator=new Accumulator<List,T>(){
    @Override public List accumulate(    List accumulated,    T in){
      accumulated.add(in);
      return accumulated;
    }
  }
;
  return new Pair<>(init,accumulator);
}
