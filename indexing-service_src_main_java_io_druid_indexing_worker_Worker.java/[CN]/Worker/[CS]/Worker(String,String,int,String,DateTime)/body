{
  this.host=host;
  this.ip=ip;
  this.capacity=capacity;
  this.version=version;
  this.lastCompletedTaskTime=new AtomicReference<>(lastCompletedTaskTime == null ? DateTime.now() : lastCompletedTaskTime);
}
