{
  Assert.assertEquals(ImmutableList.of(),select(new BoundFilter(new BoundDimFilter("dim1","2","2",true,false,true))));
  Assert.assertEquals(ImmutableList.of(),select(new BoundFilter(new BoundDimFilter("dim1","2","2",true,true,true))));
  Assert.assertEquals(ImmutableList.of(),select(new BoundFilter(new BoundDimFilter("dim1","2","2",false,true,true))));
}
