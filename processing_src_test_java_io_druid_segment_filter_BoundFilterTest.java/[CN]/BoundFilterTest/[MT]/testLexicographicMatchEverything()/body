{
  final List<BoundFilter> filters=ImmutableList.of(new BoundFilter(new BoundDimFilter("dim0","","z",false,false,false)),new BoundFilter(new BoundDimFilter("dim1","","z",false,false,false)),new BoundFilter(new BoundDimFilter("dim2","","z",false,false,false)),new BoundFilter(new BoundDimFilter("dim3","","z",false,false,false)));
  for (  BoundFilter filter : filters) {
    Assert.assertEquals(ImmutableList.of(0,1,2,3,4,5),select(filter));
  }
}
