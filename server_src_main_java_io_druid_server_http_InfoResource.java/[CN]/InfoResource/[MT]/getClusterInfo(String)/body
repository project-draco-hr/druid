{
  if (full != null) {
    return Response.ok(serverInventoryView.getInventory()).build();
  }
  return Response.ok(Lists.newArrayList(Iterables.transform(serverInventoryView.getInventory(),simplifyClusterFn))).build();
}
