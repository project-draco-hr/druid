{
  List<EventHolder> values=Lists.newArrayListWithCapacity(pQueue.size());
  while (!pQueue.isEmpty()) {
    values.add(pQueue.remove());
  }
  return new Result<SelectResultValue>(timestamp,new SelectResultValue(values));
}
