{
  final File outDir=IndexMerger.append(ImmutableList.<IndexableAdapter>of(new QueryableIndexIndexableAdapter(IndexIO.loadIndex(inDir))),tmpDir,INDEX_SPEC);
  IndexIO.DefaultIndexIOHandler.validateTwoSegments(persistTmpDir,outDir);
  return outDir;
}
