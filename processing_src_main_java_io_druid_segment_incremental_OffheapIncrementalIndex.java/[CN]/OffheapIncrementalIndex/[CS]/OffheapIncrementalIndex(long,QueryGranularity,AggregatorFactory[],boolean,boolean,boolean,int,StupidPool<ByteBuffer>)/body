{
  this(new IncrementalIndexSchema.Builder().withMinTimestamp(minTimestamp).withQueryGranularity(gran).withMetrics(metrics).withRollup(IncrementalIndexSchema.DEFAULT_ROLLUP).build(),deserializeComplexMetrics,reportParseExceptions,sortFacts,maxRowCount,bufferPool);
}
