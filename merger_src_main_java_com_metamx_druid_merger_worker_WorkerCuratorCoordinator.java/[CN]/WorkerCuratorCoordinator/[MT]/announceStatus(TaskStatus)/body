{
synchronized (lock) {
    if (!started) {
      return;
    }
    try {
      curatorFramework.create().withMode(CreateMode.EPHEMERAL).forPath(getStatusPathForId(status.getId()),jsonMapper.writeValueAsBytes(status));
    }
 catch (    Exception e) {
      throw Throwables.propagate(e);
    }
  }
}
