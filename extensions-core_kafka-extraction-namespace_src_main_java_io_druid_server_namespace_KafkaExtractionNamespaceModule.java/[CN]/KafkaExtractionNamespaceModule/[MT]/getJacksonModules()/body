{
  return ImmutableList.<Module>of(new SimpleModule("kafka-lookups"){
    @Override public void setupModule(    SetupContext setupContext){
      setupContext.registerSubtypes(KafkaExtractionNamespace.class);
      super.setupModule(setupContext);
    }
  }
);
}
