{
  ArrayList<Object[]> params=new ArrayList<>();
  params.add(new Object[]{new OffHeapNamespaceExtractionCacheManager(lifecycle,new NoopServiceEmitter(),CACHE_FACTORIES)});
  params.add(new Object[]{new OnHeapNamespaceExtractionCacheManager(lifecycle,new NoopServiceEmitter(),CACHE_FACTORIES)});
  return params;
}
