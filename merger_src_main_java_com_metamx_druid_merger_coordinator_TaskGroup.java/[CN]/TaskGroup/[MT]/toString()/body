{
  return Objects.toStringHelper(this).add("groupId",groupId).add("dataSource",dataSource).add("interval",interval).add("version",version).add("taskSet",Lists.newArrayList(Iterables.transform(taskSet,new Function<Task,Object>(){
    @Override public Object apply(    Task task){
      return task.getId();
    }
  }
))).toString();
}
