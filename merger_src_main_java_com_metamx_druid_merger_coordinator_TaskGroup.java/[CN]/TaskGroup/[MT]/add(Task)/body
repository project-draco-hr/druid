{
  Preconditions.checkArgument(task.getGroupId().equals(groupId),"Task group id[%s] != TaskGroup group id[%s]",task.getGroupId(),groupId);
  if (taskMap.containsKey(task.getId())) {
    return false;
  }
 else {
    taskMap.put(task.getId(),task);
    return true;
  }
}
