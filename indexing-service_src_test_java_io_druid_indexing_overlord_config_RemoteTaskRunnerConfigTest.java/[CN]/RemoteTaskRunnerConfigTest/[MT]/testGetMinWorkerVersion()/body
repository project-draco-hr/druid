{
  final String version="some version";
  Assert.assertEquals(version,reflect(generateRemoteTaskRunnerConfig(DEFAULT_TIMEOUT,DEFAULT_TIMEOUT,version,DEFAULT_MAX_ZNODE,DEFAULT_TIMEOUT,DEFAULT_PENDING_TASKS_RUNNER_NUM_THREADS)).getMinWorkerVersion());
}
