{
  return new Comparator<SearchHit>(){
    @Override public int compare(    SearchHit s,    SearchHit s1){
      int res=s.getValue().length() - s1.getValue().length();
      if (res == 0) {
        return (s.getValue().compareTo(s1.getValue()));
      }
      return res;
    }
  }
;
}
