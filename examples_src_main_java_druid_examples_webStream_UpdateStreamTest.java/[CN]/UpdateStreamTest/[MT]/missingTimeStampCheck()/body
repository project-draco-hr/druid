{
  testCaseSupplier=new TestCaseSupplier("{\"item1\": \"value1\"," + "\"item2\":2}");
  UpdateStream updateStream=new UpdateStream(testCaseSupplier,timeDimension);
  updateStream.run();
  Assert.assertEquals(updateStream.getQueueSize(),0);
}
