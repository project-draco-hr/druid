{
  final MergeSequence<Integer> seq1=makeUnorderedMergedSequence(Ordering.<Integer>natural(),Lists.newArrayList(TestSequence.create(1)));
  Yielder<Integer> yielder=seq1.toYielder(null,new YieldingAccumulator<Integer,Integer>(){
    @Override public Integer accumulate(    Integer accumulated,    Integer in){
      yield();
      return in;
    }
  }
);
  Assert.assertFalse(yielder.isDone());
}
