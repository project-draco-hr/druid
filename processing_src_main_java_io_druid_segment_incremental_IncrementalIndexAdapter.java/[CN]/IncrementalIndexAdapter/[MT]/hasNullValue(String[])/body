{
  if (dimValues == null || dimValues.length == 0) {
    return true;
  }
  for (  String dimVal : dimValues) {
    if (Strings.isNullOrEmpty(dimVal)) {
      return true;
    }
  }
  return false;
}
