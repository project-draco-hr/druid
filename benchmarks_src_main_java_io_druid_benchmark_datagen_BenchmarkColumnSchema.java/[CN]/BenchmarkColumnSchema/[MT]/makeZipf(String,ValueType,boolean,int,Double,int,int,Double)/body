{
  BenchmarkColumnSchema schema=new BenchmarkColumnSchema(name,type,isMetric,rowSize,nullProbability,ValueDistribution.ZIPF);
  schema.startInt=startInt;
  schema.endInt=endInt;
  schema.zipfExponent=zipfExponent;
  return schema;
}
