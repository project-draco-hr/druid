{
  List<SegmentAnalysis> results=Sequences.toList(runner1.run(testQuery,Maps.newHashMap()),Lists.<SegmentAnalysis>newArrayList());
  Assert.assertEquals(Arrays.asList(expectedSegmentAnalysis1),results);
}
