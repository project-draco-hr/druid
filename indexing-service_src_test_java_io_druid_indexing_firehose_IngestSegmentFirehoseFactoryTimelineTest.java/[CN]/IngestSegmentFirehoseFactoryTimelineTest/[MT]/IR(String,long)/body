{
  return new MapBasedInputRow(new DateTime(timeString).getMillis(),Arrays.asList(DIMENSIONS),ImmutableMap.<String,Object>of(TIME_COLUMN,new DateTime(timeString).toString(),DIMENSIONS[0],"bar",METRICS[0],metricValue));
}
