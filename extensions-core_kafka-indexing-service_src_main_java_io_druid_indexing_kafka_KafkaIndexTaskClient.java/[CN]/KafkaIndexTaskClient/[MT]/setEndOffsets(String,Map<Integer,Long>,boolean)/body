{
  try {
    submitRequest(id,HttpMethod.POST,"offsets/end",resume ? "resume=true" : null,jsonMapper.writeValueAsBytes(endOffsets),true);
  }
 catch (  IOException e) {
    throw Throwables.propagate(e);
  }
}
