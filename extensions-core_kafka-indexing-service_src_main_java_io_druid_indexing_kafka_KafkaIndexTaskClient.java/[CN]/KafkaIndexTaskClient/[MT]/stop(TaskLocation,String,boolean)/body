{
  try {
    final URI serviceUri=new URI("http",null,location.getHost(),location.getPort(),String.format("%s/%s/%s",BASE_PATH,id,"stop"),publish ? "publish=true" : null,null);
    submitRequest(new Request(HttpMethod.POST,serviceUri.toURL()),true);
  }
 catch (  Exception e) {
    throw Throwables.propagate(e);
  }
}
