{
  this.httpClient=httpClient;
  this.jsonMapper=jsonMapper;
  this.retryPolicyFactory=new RetryPolicyFactory(new RetryPolicyConfig().setMinWait(Period.seconds(2)).setMaxWait(Period.seconds(8)).setMaxRetryCount(5));
}
