{
  return String.format("URIExtractionNamespace = { namespace = %s, uri = %s, namespaceParseSpec = %s, pollPeriod = %s }",namespace,uri.toString(),namespaceParseSpec.toString(),pollPeriod.toString());
}
