{
  if (dimVals == null || dimVals.isEmpty()) {
    return false;
  }
  for (  String dimVal : dimVals) {
    if (dimVal == null || Floats.tryParse(dimVal) == null) {
      return false;
    }
  }
  return true;
}
