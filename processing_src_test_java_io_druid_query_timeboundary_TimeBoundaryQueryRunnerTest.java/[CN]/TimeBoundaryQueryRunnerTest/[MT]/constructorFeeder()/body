{
  return QueryRunnerTestHelper.makeQueryRunners(new TimeBoundaryQueryRunnerFactory(new QueryWatcher(){
    @Override public void registerQuery(    Query query,    ListenableFuture future){
    }
  }
));
}
