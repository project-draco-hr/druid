{
  final Committed committed=fixedInstance();
  final Committed committed2=Committed.create(ImmutableMap.of(IDENTIFIER_OBJECT1,3,IDENTIFIER_OBJECT2,2),ImmutableMap.of("metadata","foo"));
  Assert.assertEquals(committed,committed2);
}
