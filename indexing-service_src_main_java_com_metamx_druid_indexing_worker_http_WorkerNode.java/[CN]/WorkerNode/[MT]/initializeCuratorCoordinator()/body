{
  if (workerCuratorCoordinator == null) {
    workerCuratorCoordinator=new WorkerCuratorCoordinator(getJsonMapper(),getZkPaths(),null,getCuratorFramework(),null);
    getLifecycle().addManagedInstance(workerCuratorCoordinator);
  }
}
