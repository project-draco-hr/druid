{
  return new JavaScriptBufferAggregator(Lists.transform(fieldNames,new com.google.common.base.Function<String,ObjectMetricSelector>(){
    @Override public ObjectMetricSelector apply(    @Nullable String s){
      return metricSelectorFactory.makeObjectColumnSelector(s);
    }
  }
),compiledScript);
}
