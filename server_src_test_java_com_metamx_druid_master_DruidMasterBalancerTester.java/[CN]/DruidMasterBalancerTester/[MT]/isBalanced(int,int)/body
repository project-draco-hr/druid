{
  if (serverMap.size() == numServers) {
    for (    int numSegments : serverMap.values()) {
      if (numSegments < min) {
        return false;
      }
    }
    return true;
  }
  return false;
}
