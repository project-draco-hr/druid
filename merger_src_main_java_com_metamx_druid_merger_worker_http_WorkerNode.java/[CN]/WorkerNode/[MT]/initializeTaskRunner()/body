{
  if (forkingTaskRunner == null) {
    forkingTaskRunner=new ForkingTaskRunner(configFactory.build(ForkingTaskRunnerConfig.class),Executors.newFixedThreadPool(workerConfig.getCapacity()),getJsonMapper());
  }
}
