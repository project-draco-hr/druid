{
  if (workerCuratorCoordinator == null) {
    workerCuratorCoordinator=new WorkerCuratorCoordinator(getJsonMapper(),getConfigFactory().build(IndexerZkConfig.class),getCuratorFramework(),new Worker(workerConfig));
    getLifecycle().addManagedInstance(workerCuratorCoordinator);
  }
}
