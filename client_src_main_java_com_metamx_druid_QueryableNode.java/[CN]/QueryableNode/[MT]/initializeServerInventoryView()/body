{
  if (serverInventoryView == null) {
    serverInventoryView=new ServerInventoryView(getConfigFactory().build(ServerInventoryViewConfig.class),getZkPaths(),getCuratorFramework(),getJsonMapper());
    lifecycle.addManagedInstance(serverInventoryView);
  }
}
