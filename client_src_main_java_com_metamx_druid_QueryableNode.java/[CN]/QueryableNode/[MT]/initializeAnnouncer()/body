{
  if (announcer == null) {
    setAnnouncer(new CuratorDataSegmentAnnouncer(getDruidServer(),getZkPaths(),new Announcer(getCuratorFramework(),Execs.singleThreaded("Announcer-%s")),getJsonMapper()));
    lifecycle.addManagedInstance(getAnnouncer());
  }
}
