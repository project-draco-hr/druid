{
  try {
    makeRequest(HttpMethod.DELETE,String.format("%sdatasources/%s/intervals/%s?kill=%s",getCoordinatorURL(),dataSource,interval.toString().replace("/","_"),kill));
  }
 catch (  Exception e) {
    throw Throwables.propagate(e);
  }
}
