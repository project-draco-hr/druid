{
  return new ConcurrentSkipListSet<>(Collections2.transform(segmentsToLoad.keySet(),new Function<SegmentHolder,DataSegment>(){
    @Override public DataSegment apply(    SegmentHolder input){
      return input.getSegment();
    }
  }
));
}
