{
  return new ConcurrentSkipListSet<>(Collections2.transform(segmentsToDrop.keySet(),new Function<SegmentHolder,DataSegment>(){
    @Override public DataSegment apply(    SegmentHolder input){
      return input.getSegment();
    }
  }
));
}
