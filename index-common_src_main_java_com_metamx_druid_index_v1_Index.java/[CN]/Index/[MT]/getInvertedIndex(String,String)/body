{
  final Map<String,Integer> lookup=dimIdLookup.get(dimension);
  if (lookup == null) {
    return emptySet;
  }
  final Integer integer=lookup.get(value);
  if (integer == null) {
    return emptySet;
  }
  try {
    return indexes.get(dimension)[integer];
  }
 catch (  NullPointerException e) {
    log.warn(e,"NPE on dimension[%s], value[%s], with index over interval[%s]",dimension,value,dataInterval);
    return emptySet;
  }
}
