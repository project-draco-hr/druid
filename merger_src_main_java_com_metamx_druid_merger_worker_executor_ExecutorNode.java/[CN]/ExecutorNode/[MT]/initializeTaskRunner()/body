{
  if (taskRunner == null) {
    this.taskRunner=lifecycle.addManagedInstance(new ExecutorServiceTaskRunner(taskToolboxFactory,Executors.newSingleThreadExecutor(new ThreadFactoryBuilder().setNameFormat("task-runner-%d").build())));
  }
}
